import { AppShell } from "@/components/layout/app-shell";
import { VerificationEngine } from "@/components/verification/verification-engine";

export default function VerificationEnginePage() {
  return (
    <AppShell>
      <VerificationEngine />
    </AppShell>
  );
}
